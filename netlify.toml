# AdMonitor dashboard — static site. The worker (GitHub Actions) writes public/state.json
# and redeploys this after each daily run. No functions / no scheduled scraping here.
[build]
  publish = "public"

[functions]
  directory = "netlify/_none"   # intentionally empty so no functions deploy
